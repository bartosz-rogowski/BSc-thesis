Particle:
	Position position;
	Velocity velocity;
	boolean/int iscatter = 0;
	double r_eff = stała dla wszystkich obiektów?;

Position:
	double x;
	double y;

	Warunki: (pudełko numeryczne)
		0 < x < x_max
		0 < y < y_max


Velocity:
	double x;
	double y;

	Warunki: 
		x i y: x^2 + y^2 = 10 (lub inna zadana liczba)

Cell:
	int i;
	int j;
	Particle[] particles;


Particle.toCell():
	this.position.x -map-> i : floor(x/dx) + 1
	this.position.y -map-> j : floor(y/dy) + 1 //to +1 jeśli w [0] będziemy przechowywać length
	// sprawdzanie po każdym przemieszczeniu
	



